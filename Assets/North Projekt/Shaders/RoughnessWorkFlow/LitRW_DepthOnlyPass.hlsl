// LitRW_DepthOnlyPass.hlsl
#ifndef LIT_RW_DEPTHONLY_PASS_INCLUDED
#define LIT_RW_DEPTHONLY_PASS_INCLUDED

#include "Packages/com.unity.render-pipelines.universal/Shaders/LitInput.hlsl"
#include "Packages/com.unity.render-pipelines.universal/Shaders/DepthOnlyPass.hlsl"

// All keywords are defined in the .shader file.
// We can directly use the URP functions as they only depend on alpha clipping.

#endif